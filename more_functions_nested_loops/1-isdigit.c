#include "main.h"
/**
 * isdigit- checks to see if char is digit
 * @c: input integer
 *
 * Return: 1 if digit otherwise 0
 */
int _isdigit(int c)
{
	if (c < 49 && c >= 39)
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
